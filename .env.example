# WordPress Configuration (must include /wp-json/wp/v2 path)
# Example: https://about.fb.com/wp-json/wp/v2
WP_API_URL=https://your-wordpress-site.com/wp-json/wp/v2

# Optional: WordPress authentication for private content
# WP_USERNAME=your_username
# WP_APP_PASSWORD=your_application_password

# Contentful Configuration
CONTENTFUL_SPACE_ID=your_space_id
CONTENTFUL_MANAGEMENT_TOKEN=your_management_token
CONTENTFUL_DELIVERY_TOKEN=your_delivery_token
CONTENTFUL_ENVIRONMENT=master

# Migration Options
BATCH_SIZE=5
DELAY_MS=1000
# MAX_ASSET_SIZE_MB=500   # Max file size to download (default 500MB); increase for large videos
# ASSET_UPLOAD_TIMEOUT_MS=600000   # Upload timeout (default 10 min); increase for slow connections
# SKIP_ASSET_IDS=41041,41042   # Comma-separated WP media IDs to skip (for assets that timeout)
# MIGRATE_VIDEOS=false         # Videos excluded by default (large files, timeouts); set true to include
